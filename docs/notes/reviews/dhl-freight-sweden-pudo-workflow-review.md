# DHL Freight Sweden PUDO workflow lookups — review gate

Branch: dhl-freight-sweden-pudo-workflow. Scope: 972e47403..HEAD (nine commits) against `PRDs/DHL_FREIGHT_SWEDEN_PUDO_WORKFLOW.md` v1.1 Implemented. Fresh-context review of the two connector-local lookup capabilities (`find_product_matches`, `find_service_points`), the `LocationType` enum with the subType mapping, URL properties, error-parser extension, README calling conventions, and the hermetic test suite.

## Review summary

### Status: PASS

### Verification performed

- Connector suite: 49/49 tests pass in 0.04 s (`source bin/activate-env && python -m unittest discover -v -f modules/connectors/dhl_freight_sweden/tests`), confirming hermeticity (no network call completes that fast; transport is mocked `lib.request` throughout).
- Full SDK sweep: `./bin/run-sdk-tests` exit 0, "All SDK tests completed successfully" (run in the background during this review).
- Capability path: `karrio.references.get_carrier_capabilities` (references.py:867-881) read together with the server consumers (`modules/core/karrio/server/providers/signals.py:24` and `serializers/base.py:211/244/307/343`) to confirm the stability test asserts through the exact path the server uses; `CarrierCapabilities.map_capability` substring rules checked against both new method names.
- Probe script (executed against this worktree, module provenance asserted): zero-valued distance, partial address dicts, empty `parcels`, healthy and in-band-error locator bodies, defensive `BadRequestError` bodies through both providers, and the README example run end-to-end with mocked transport.
- Country-list reformat: an `ast`-based script extracted every string-list constant from `units.py` and `test_services.py` at 972e47403 and at HEAD and compared them as ordered lists.
- Fixture fidelity: the Warsaw fixtures cross-checked field-by-field against `docs/notes/evidence/dhl-freight-sweden-pudo-lookups-live-capture.md` and the PRD pending-question #1 row.
- Formatting and hygiene: `black --check` on all ten touched Python files; grep sweeps for legacy `DP`/`SF`/`NF` utilities, bare `except`, mutable defaults, pytest, and secret-like strings across the full diff.

### Findings

1. [PASS] PRD compliance, normalized shapes. The product dict carries every field of the PRD output table (code, name, short_name, the four booleans, from_countries, to_countries, to_country_postal_excludes, payer_codes, transportation_mode, sub_categories, rules_for_country_delivery_types) with the exact source fields named in the table, and the service-point dict likewise (both identifiers, name, shop_name, type, the four-key address, coordinates, distance, distance_unit, service_types). Verified field-for-field against the PRD data-flow tables, not by name alone.
2. [PASS] PRD compliance, decision #4 guard. `product_matches_request` raises before any carrier call when either party lacks postal+country, with code `SHIPPING_SDK_FIELD_ERROR`, a message naming Consignor and Consignee, and per-party details; the guard test asserts all three. The custom `ShippingSDKDetailedError` subclass matches the connector's existing `ServicePointDetailsError` idiom (core defines no FieldError class).
3. [PASS] PRD compliance, decisions #1, #5, #6, #7. The diff's thirteen files are exactly the connector's providers, proxy, units, utils, error, README, its tests, the PRD, and the evidence note; no capability, METADATA, server, or dashboard file appears, and `mapper.py` plus the generated schemas are untouched. Both identifiers are exposed on every normalized point. The subType mapping was executed programmatically: locker maps to ParcelStation, the other three location types to ParcelShop, and every enum value is a key.
4. [PASS] Capability stability through the server's own path. `get_carrier_capabilities` derives capabilities from the proxy class's own public methods via substring rules ("rate", "tracking", "shipment", "pickup", "address", "document", "manifest", "duties"); neither `find_product_matches` nor `find_service_points` contains any of them, so `map_capability` returns None for both, and the test's assertion `{"shipping", "rating"}` passes against the same function `signals.py` and the connection serializers call.
5. [PASS] Test coverage. All nine PRD test-method names exist (five product-matches including the guard, four service-points) plus `test_capabilities_unchanged_by_lookup_methods`, each asserts what the PRD table claims, the suite is unittest throughout, and the full SDK sweep is green. Fixtures are module-level constants with a `cached` gateway per the repo pattern.
6. [PASS] Adversarial probe, falsy values. `lib.to_dict` strips None and empty strings but not numeric zero: `{"value": 0, "unit": "km"}` serializes to `"distance": 0, "distanceUnit": "km"` on the wire, so no falsy-but-meaningful filter is lost. Partial address dicts simply omit the absent keys, and an empty `parcels: []` yields no `pieces` key (empty collections are stripped), which is semantically identical to omitting piece criteria; all consistent with the repo-wide `to_dict` convention.
7. [PASS] Adversarial probe, error parser on full bodies. A healthy `status: "OK"` body with populated `servicePoints` parses to zero Messages: `_extract_errors` only inspects top-level `errorMessage`/`validationErrors`/`error`/`errors`, so point fields cannot trigger it. An in-band `{"status": "Error", "errorMessage": ...}` body yields exactly one Message with code "Error", and a defensive `BadRequestError {error, errors[]}` body remaps and yields exactly one Message carrying the errorCode and field detail through both providers. The remap branch is inert for every pre-existing fixture shape (none carries top-level `error`/`errors`), so the shared-parser extension cannot regress booking or print flows.
8. [PASS] Adversarial probe, country-list reformat. The black explosion of the packed lists changed layout only: ordered comparison of all six constants (PARCEL_CONNECT_B2C/PLUS/EUROCONNECT_PLUS in `units.py` and their three test mirrors, lengths 24/23/26) between 972e47403 and HEAD is identical member-for-member and order-for-order, so the zone drift guard locks exactly the same data. The touched files were not black-clean before this range; the change brings them into compliance, and `black --check` passes on all ten.
9. [PASS] Adversarial probe, fixture fidelity. The parcelshop fixture (SENATORSKA 2, 00-075 WARSZAWA, 0.06 km, id 101, ŻABKA) falls inside call 1's recorded parcelshop set (five points, 0.06-0.39 km) and the parcelstation fixture (Plac Bankowy 2, 00-095, 0.66 km, id 501) matches the nearer of call 2's two lockers (0.66/1.08 km). The PL `id` semantics in pending question #1 (101 a constant parcelshop code, 501 a constant parcelstation code, `servicePointId` the unique identifier) is consistent across the PRD row, the evidence note, and both fixtures. The raw captures are deliberately not committed, so verbatim-ness rests on the evidence note; internal consistency is complete.
10. [PASS] Adversarial probe, README example. The documented sequence was executed against the shipped code with mocked transport: the package re-exports make `product_matches` and `service_points` importable from `karrio.providers.dhl_freight_sweden`, both request builders accept exactly the documented dict keys, both proxy methods exist with those names, both parse functions return the `(list, messages)` pair, and the README's option references (the five service-point options, `service_point_type` ParcelStation/ParcelShop) match the `units.py` option enums and `SUB_TYPE_BY_LOCATION_TYPE`. The example is runnable as written.
11. [PASS] Code quality and security. `karrio.lib as lib` everywhere with no legacy utilities, functional comprehensions throughout, no bare exceptions or mutable defaults, proxy methods stylistically identical to the existing `create_shipment` and the postnord precedent (same headers, `trace_as`, `on_error=lib.error_decoder`, `Deserializable(response, lib.to_dict)`). No secret appears in the diff (only the `TEST_CLIENT_KEY` placeholder), the evidence note names the client-key header without its value, and the suite is hermetic.
12. [WARN] Silent empty result for `status: "Error"` without `errorMessage`. The locator's declared failure shape always carries `errorMessage`, and a body with `status: "Error"` but no `errorMessage` (and no error keys) parses to zero points and zero Messages, i.e. an empty success. Outside the spec's declared shapes, so not a defect against the PRD; worth a line in the README if a caller ever hits an unexplained empty result.
13. [WARN] Fixtures below `unittest.main()` in the two new test files. `test_product_matches.py` and `test_service_points.py` place their fixture constants after the `if __name__ == "__main__": unittest.main()` block, so direct file execution would fail with a NameError before the constants are defined; discovery, the documented invocation, is unaffected. This replicates a pattern the zone-coverage review already flagged, and this range incidentally fixed its `test_services.py` instance by moving that block to the end of the file.
14. [WARN] PRD input table omits the `total_*` keys. The productmatches builder accepts `total_volume`, `total_number_of_pieces`, `total_loading_meters`, `total_pallet_places`, and `total_weight`, and the test fixture exercises two of them because the evidence note fixes the fixture request as the exact live-captured body; the PRD's input-payload table lists only shipper/recipient, parcels, and import_export. The code, fixture, and capture agree, so the PRD table is the incomplete artifact; a v1.2 row would close the gap.

### Required actions

None. The warnings in findings 12-14 are documentation-level observations, not merge blockers.
