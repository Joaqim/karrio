## 1. Schema generation

- [x] 1.1 Extend `modules/connectors/postnord/schemas/shipment_request.json` with the customs branches (`customsDeclarationCN22`, `customsDeclarationCN23`, `customsInvoice` and their nested definitions, lifted from `vendor/booking.swagger.json` SW:5176+, 5320+, 5411+, 5461+, 5942-6036) on the shipment entries, and verify `./bin/run-generate-on modules/connectors/postnord` regenerates with the customs types present on the shipment request model
- [x] 1.2 Add `schemas/customs_declaration_request.json` (declaration envelope: `ids[]` with `idType`, one-of CN22/CN23/customsInvoice branches, `updateIndicator`) and `schemas/customs_declaration_response.json` (`bookingResponseCN` with per-id `OK|FAIL` status, `labelPrintout`), plus the `ids_label` request fragment for `/v3/labels/ids/{pdf,zpl}`, and verify the regenerated modules import cleanly and round-trip a sample declaration payload via `lib.to_dict`

## 2. Booking-time customs carriage (design D1, D5)

- [x] 2.1 Implement the unified-customs → `customsDeclarationCN22` mapping (field table in design.md) in the shipment request builder, and verify a new unit test asserting the full booking request body for a payload with customs matches an expected `RequestData` fixture
- [x] 2.2 Verify bookings without customs keep the exact pre-change request shape by leaving all existing `test_shipment.py` request assertions unmodified and green
- [x] 2.3 Add the `CUSTOMS_DECLARATION_MAX_LINES = 13` pre-submission guard to the booking path, and verify a unit test asserting a 14-line payload fails with a field error naming the limit and `lib.request` is never called
- [x] 2.4 Map sender registration numbers from `customs.options` (`eori_number` → `EORIorPersonalIdNumber`, `voec_number` → `voec`, `ioss_number` → `ioss`; absent options send nothing — live finding `SACUS-BR-24062502`), and verify unit tests covering presence, absence-keeps-shape, and the live script threading (`POSTNORD_EORI`/`POSTNORD_VOEC`/`POSTNORD_IOSS` env → `customs.options`)

## 3. Implicit standalone customs document for UX (design D2)

- [x] 3.1 Parse `printoutComposition` in `_extract_details` and carry the non-zero document kinds into the parsed result, and verify a unit test against the existing response fixture asserts the composed kinds (cn22 etc.) are extracted
- [x] 3.2 Add the post-booking by-id fetch to `create_shipment` in `karrio/mappers/postnord/proxy.py`: when the booked service is `postnord_export_letter` and customs data was present, call `POST /v3/labels/ids/pdf` with the first assigned item id and `definePrintout=onlyCustomsDeclarations`, and attach returned printouts as `docs.extra_documents` entries with category from `printoutComposition` and format PDF, and verify a unit test with two mocked `lib.request` calls asserts the second call's URL/query and the populated `extra_documents`
- [x] 3.3 Reuse `_printout_base64` raw-UTF-8 re-encoding for the ZPL variant and verify a unit test booking with `label_type=ZPL` asserts the `/v3/labels/ids/zpl` path and a ZPL-format `ShippingDocument`
- [x] 3.4 Make the by-id fetch fail-open: booking stays successful and the retrieval failure is reported as messages, and verify a unit test with a failing second mocked call asserts the shipment result plus error messages and no exception
- [x] 3.5 Surface by-id per-id failures as messages (live finding 2026-09-21: a by-id response `[{"itemIds": [{"itemIds": ..., "status": "FAIL", "errorResponse": {"message": "id not found"}}]}]` — the swagger's `itemIds_inner` shape — currently yields zero documents AND zero messages), correcting the by-id `itemIds` schema member to the swagger object shape, and verify a unit test modeled on the live capture asserts the booking stands and the per-id failure surfaces as a message
- [x] 3.6 Key the by-id customs fetch by the booking response's `printId` rather than the item id (live finding 2026-09-21 production: the same real booking fails `id not found` keyed by item id and succeeds keyed by `assignedIds.printId`, confirming the swagger's `assignedIds.printId` description), surfacing an OK-but-empty by-id response for a customs-declared booking as a message rather than silence (live capture: status OK, all-zero `printoutComposition`, no printout data), correcting the sibling `customs_declaration_response.json` `labelPrintout.itemIds` member to the same object shape (spec-review confirmation: pre-existing string-array gap, PDF-variant declaration path), and verify unit tests assert the printId-keyed request body and the empty-result message

## 4. Post-booking declaration proxy (design D3)

- [x] 4.1 Create `karrio/providers/postnord/customs.py` with the declaration request builder (enforcing `ids[]` maxItems 1, exactly one branch, and the 13-line guard) and parsers for the digital and PDF-variant responses (`bookingResponseCN` statuses; PDF variant additionally maps `labelPrintout` → `ShippingDocument` list with caller-supplied `paperSize`/`rotate`/`multiPDF` params), and verify unit tests for request building and both response parsings
- [x] 4.2 Add proxy methods `create_customs_declaration` and `create_customs_declaration_pdf` following the `find_service_points` precedent, and verify unit tests (patched `lib.request`) assert endpoint URLs, methods, bodies, and parsed results, including an upstream rejection surfaced as unified error messages
- [x] 4.3 Verify the 13-line guard applies to the proxy path with a unit test asserting the field error and no HTTP call for an over-limit declaration, reusing one shared guard helper beside `CUSTOMS_DECLARATION_MAX_LINES` rather than duplicating the booking path's check (design D5)
- [x] 4.4 Document consumer usage (invocation via `gateway.proxy.create_customs_declaration(...)`, branch choice, consumer-owned correctness) in `docs/notes/` following the service-points PRD pattern, including the string-passing convention for digit-coerced schema fields (never `int()`-coerce; leading-zero and alphanumeric values must pass through) and the `BuyerType`-union caveat for party construction, and verify the note exists with a runnable example

## 5. Verification and release prep

- [x] 5.1 Run the full connector suite `python -m unittest discover -v -f modules/connectors/postnord/tests` and verify all tests pass, then `./bin/run-sdk-tests` to confirm no cross-connector regressions
- [x] 5.2 Live-verify in the PostNord test environment: book `UX` with customs and confirm the CN22 appears in `printoutComposition`, the by-id `onlyCustomsDeclarations` fetch returns a real PDF and ZPL printout, and a 14-line declaration is rejected; record findings in `docs/notes/` (first round 2026-09-21: auth probe + 13-line guard PASS; bookings blocked pending 2.4 registration numbers, `SACUS-BR-24062502`; second round 2026-09-21 after 2.4: bookings PASS with registration threaded and `cn22` composed into the booking printout; test-mode bookings never propagate to the print/declaration subsystems — by-id `id not found` and declaration `SAO-BR-24041806`, identical at +5 min; production probes on real booking UX478114854SE: by-id keyed by the item id fails `id not found` while the printId key succeeds, and `itemIds[].reference` is the references object shape; live CN22 creation on the existing booking pending; third round 2026-09-21 production: declaration POST accepted on UX478114854SE — bare `bookingResponseCN` success envelope (I1 fix live-confirmed), rejected without EORI (`SACUS-BR-24062502` spans the post-booking declaration endpoint — add to the 4.4 consumer note), intra-EU SE→PL accepted, response echoes `idType: ITEMID` uppercase where booking responses use `itemId`; fourth round 2026-09-21 production: standalone CN22 retrieval confirmed — by-id `onlyCustomsDeclarations` keyed by printId returns composition `{'cn22': 1}` with a real 16.7 KB PDF, and the unrestricted fetch returns `{'label': 1, 'cn22': 1}` (composed-printout duplication live-confirmed as designed); residuals: live ZPL printout and the server-side 14-line rejection remain unprobed — both unit-tested, sandbox test-mode cannot reach them, noted in the findings record; fifth round 2026-09-21 production: candy-order re-declaration via `updateIndicator: "Update"` accepted on UX478114854SE ("SALE OF GOODS" categoryType, EUR) — PDF-variant endpoint returns the wrapped `{bookingResponse, labelPrintout}` envelope matching the modeled schema, with the rendered pre-filled CN22 (17,040 chars) saved for eye verification, and the post-update by-id ZPL fetch returns raw ZPL text in `printout.data` (2,866 chars, `^XA` magic), live-confirming the `_printout_base64` raw-UTF-8 re-encoding design — sole remaining residual: server-side 14-line rejection)
- [x] 5.3 Add the changelog entry (Features) noting that customs data is no longer dropped at booking and that `UX` bookings now return a standalone customs document
- [x] 5.4 Run the fresh-context review gate against the spec, design, and repo checklists (PRD compliance, test coverage, karrio.lib usage, tenant isolation, no hardcoded strings) and address findings
